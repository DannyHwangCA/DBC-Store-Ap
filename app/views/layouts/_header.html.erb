    <nav class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <div class="navbar-header">
          <%= link_to "Rails Shop", root_url, class: "navbar-brand" %>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="active"><%= link_to "Home", root_url %></li>
            <% if current_user %>
              <%if is_admin? %>
                <li><%= link_to "Admin", admin_path %></li>
              <% end %>

              <li><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
              <li class="active" id="kart-button" >
                <%= link_to "Shopping Kart (#{current_user.carts.count}) " %>
              </li>

            <% else %>
              <li><%= link_to 'Login', new_session_path %></li>
              <li><%= link_to "Sign up", new_user_path %></li>
            <% end %>
          </ul>
        </div><!-- /.nav-collapse -->
      </div><!-- /.container -->
    </nav><!-- /.navbar -->
